// test.si

let a = 5

fn add {
  let b = 10
  return a + b
}

fn add_local {
  let a = 7 // Locally assigned variables have precedence over variables assigned in parent scopes
  let b = 10
  return a + b
}