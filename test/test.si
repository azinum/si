// test.si

import math;
import basic_tests;


let some_value = 5;

fn test(a) {
  fn get_value(to_add) {
    return some_value + to_add;
  }
  if a == 0 {
    return get_value(5);
  }
  return get_value(10);
}

